# shoresh service deps
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0          # spine source downloads + bcv-corpus client + Cloudflare API
python-dotenv>=1.0.0

# clause search — vectors loaded from /data volume at startup
numpy>=1.26.0
